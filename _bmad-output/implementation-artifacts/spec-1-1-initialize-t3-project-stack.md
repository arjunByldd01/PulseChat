---
title: '1.1 Initialize T3 Project Stack'
type: 'chore'
created: '2026-05-03'
status: 'done'
baseline_commit: 'NO_VCS'
context: []
---

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** No project exists yet — the team cannot build any feature without a correctly wired T3 foundation (Next.js 15, Prisma, NextAuth.js v5, Tailwind, Shadcn/ui) plus a running Socket.io dev process.

**Approach:** Bootstrap via `create-t3-app` (no tRPC, App Router), add Shadcn/ui and Socket.io dependencies, update the `dev` script to run Next.js and the Socket.io server concurrently, verify TypeScript and ESLint pass clean.

## Boundaries & Constraints

**Always:**
- Use `npm create t3-app@latest slack-clone` — select TypeScript, Tailwind, Prisma, NextAuth, **no tRPC**, App Router.
- Run `npx shadcn@latest init` immediately after: Default style, CSS variables enabled, `src/app` path.
- Install runtime deps: `socket.io socket.io-client ioredis @socket.io/redis-adapter concurrently`
- Install dev dep: `tsx`
- Root `package.json` `"dev"` script must be exactly: `"concurrently \"next dev\" \"tsx watch server/index.ts\""`
- Create `server/index.ts` as an empty scaffold (no logic yet — that is Story 1.4).
- All output directories `src/`, `server/`, `prisma/`, `public/` must exist after setup.
- `tsc --noEmit` and `npm run lint` must both exit 0 before story is done.

**Ask First:**
- If `create-t3-app` prompts for options not listed above (e.g. package manager choice, import alias), pause and ask Arjun before proceeding.

**Never:**
- Do not add tRPC — it was explicitly deselected.
- Do not configure NextAuth, Prisma schema, or Docker in this story — those are Stories 1.2–1.3.
- Do not write any application logic (routes, components, handlers) — only project scaffolding.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| Dev server starts | Clean scaffold, `npm run dev` | Next.js on :3000, Socket.io process on :3001 (stub), no crash | Fix any missing dep or script error before marking done |
| TypeScript check | `tsc --noEmit` on scaffold + added files | Exit 0 | Resolve all TS errors; do not use `// @ts-ignore` to suppress |
| Lint check | `npm run lint` | Exit 0 | Resolve all ESLint errors; do not disable rules project-wide |

</frozen-after-approval>

## Code Map

- `package.json` — root manifest; `dev` script wires concurrently
- `server/index.ts` — empty Socket.io server entry (created here, implemented in 1.4)
- `src/` — Next.js app source (generated by T3)
- `prisma/schema.prisma` — Prisma schema (T3 generates stub; full schema in 1.3)
- `tsconfig.json` — TypeScript config (T3 strict mode default)
- `components.json` — Shadcn/ui config (generated by `shadcn init`)

## Tasks & Acceptance

**Execution:**
- [x] Run `npm create t3-app@latest slack-clone` in the project parent dir — select TypeScript, Tailwind, Prisma, NextAuth, no tRPC, App Router
- [x] `cd slack-clone` then run `npx shadcn@latest init` — Default style, CSS variables on, src/app path
- [x] Run `npm install socket.io socket.io-client ioredis @socket.io/redis-adapter concurrently`
- [x] Run `npm install -D tsx`
- [x] `package.json` -- update `"dev"` script to `"concurrently \"next dev\" \"tsx watch server/index.ts\""` -- required by architecture
- [x] `server/index.ts` -- create with minimal valid TS (empty export or `console.log` stub) -- prevents `tsx watch` from crashing on start
- [x] Verify `tsc --noEmit` exits 0
- [x] Verify `npm run lint` exits 0

**Acceptance Criteria:**
- Given the project directory, when `npm run dev` is run, then Next.js starts on port 3000 and the Socket.io process starts without crashing
- Given the initialized project, when `tsc --noEmit` is run, then it exits 0
- Given the initialized project, when `npm run lint` is run, then it exits 0
- Given the project root, the directories `src/`, `server/`, `prisma/`, and `public/` all exist
- Given `components.json`, Shadcn/ui is initialized (file exists and has `style: "default"`)

## Verification

**Commands:**
- `tsc --noEmit` -- expected: exit 0, no type errors
- `npm run lint` -- expected: exit 0, no lint errors
- `ls src/ server/ prisma/ public/` -- expected: all four directories present
- `cat package.json | grep -A1 '"dev"'` -- expected: concurrently command with next dev and tsx watch
